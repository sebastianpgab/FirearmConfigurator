import { Option } from "../option/model";

interface Barrel {
    calibers: Option[];
    contour: Option[];
    profiles: Option[];
    lengths: Option[];
    openSights: Option[];
    muzzleBrakesOrSuppressors: Option[];
}