import { Option } from "../option/model";

export interface ChamberBolt {
    chamberEngravings: Option[];
    boltHandles: Option[];
    triggers: Option[];
    boltHeads: Option[];
    slidingSafeties: Option[];
}
